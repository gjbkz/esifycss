{
    "private": true,
    "scripts": {
        "build:esifycss": "esifycss --output output/css.js src/**/*.css",
        "build:parcel": "parcel build src/index.html --out-dir output",
        "build": "run-s build:esifycss build:parcel"
    },
    "devDependencies": {
        "esifycss": "file:../..",
        "npm-run-all": "^4.1.5",
        "parcel-bundler": "^1.12.3"
    }
}
