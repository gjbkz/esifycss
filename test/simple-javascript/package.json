{
    "private": true,
    "scripts": {
        "build:cleanup": "rimraf output",
        "build:esifycss": "esifycss --output src/helper.css.js src/**/*.css",
        "build:parcel": "parcel build src/index.html --no-minify --out-dir output",
        "build": "run-s build:cleanup build:esifycss build:parcel"
    },
    "devDependencies": {
        "esifycss": "file:../..",
        "npm-run-all": "^4.1.5",
        "parcel-bundler": "^1.12.3",
        "rimraf": "^2.6.3"
    }
}
