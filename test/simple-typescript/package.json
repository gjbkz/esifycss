{
    "private": true,
    "scripts": {
        "build:cleanup": "rimraf output temp",
        "build:esifycss": "esifycss --output src/helper.css.ts src/**/*.css",
        "build:tsc": "tsc",
        "build:rollup": "rollup temp/page.js --format iife --file output/page.js",
        "build:html": "cpy src/index.html output",
        "build": "run-s build:cleanup build:esifycss build:tsc build:rollup build:html"
    },
    "devDependencies": {
        "cpy-cli": "^2.0.0",
        "esifycss": "file:../..",
        "npm-run-all": "^4.1.5",
        "rimraf": "^2.6.3",
        "rollup": "^1.13.1",
        "typescript": "^3.5.1"
    }
}
