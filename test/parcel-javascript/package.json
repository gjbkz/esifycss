{
    "private": true,
    "scripts": {
        "build:esifycss": "esifycss src/**/*.css",
        "build:parcel": "parcel build src/index.html --no-minify --out-dir output",
        "build": "run-s build:esifycss build:parcel"
    },
    "devDependencies": {
        "esifycss": "file:../..",
        "npm-run-all": "*",
        "parcel-bundler": "*"
    }
}
